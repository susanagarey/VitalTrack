INSTRUCCIONES:
==========================================================================================
1) Instalar base de datos y datos de prueba en base de datos MySQL:

 1.a) ejecutar script crear el esquema y el usuario: VitalTrack-Schema-ConUsuario.sql
 1.b) ejecutar script carga datos pruebas: VitalTrack-DatosDespuesPruebas.sql


nombre base datos: vitaltrack
usuario: vitaltrackadmin
contraseña: Abc123.


2) Crear carpeta de red mapeada a T: donde se guardan las fotos de los usuarios, a los
   que accede la app (para no guardarlas en la base de datos, que sería más pesado).

    Se puede simular una carpeta en T: para pruebas con, por ejemplo:
        md "C:\VitaltrackFotos\Fotos"
        subst T: "C:\VitaltrackFotos\"


3) Copiar fotos de muestra de carpeta FotosPruebas  a T:\Fotos 
   (T:\Fotos sería igual a C:\VitaltrackFotos\Fotos)

4) Ejecutar la versión de la aplicación deseada:
     A) Con instalador ClickOnce:
	     A.1) Framework-dependent (la aplicación no incluye el .NET runtime necesario 8.0-windows LTS)
		   A.1.1) Portable (no depende de SO o plataforma, para ejecutar en cualquier plataforma que tenga un .NET runtime)
		   
		   A.1.2) win-x64 (debe tener instalado el runtime .NET 8)
		 
		 A.2) Self-contained (incluye el .NET runtime necesario)
		   Se puede escoger cualquiera de las 3 subcarpetas (creé una versión de cada tipo para probar).
            	 
	 
	 B) Versión para ejecutar desde carpeta (local o de red):
	 
	    Instaladores VitalTrackApp\Folder\VitalTrack.exe


OBSERVACIONES:
==========================================================================================
Las cadenas de texto de:
 - la conexión de base de datos
 - carpeta "mapeada en red" para guardar las fotos de forma centralizada en un servidor
   se encuentran en el archivo:
   
    appsettings.json 

            {
              "ConnectionStrings": {
                "Default": "server=localhost;database=vitaltrack;user id=vitaltrackadmin;password=Abc123."
              },
              "Images": {
                "CarpetaFotos": "T:\\Fotos\\",
                "FotoPorDefecto": "desconocido.png"
              }
            }
 
 
 
 